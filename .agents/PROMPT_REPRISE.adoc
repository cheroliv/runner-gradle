= PROMPT_REPRISE — Session 007 — Engine
:toc:

== Contexte de Reprise

*Sessions 0→6 ont realise* :
- 0 : Bootstrap gouvernance
- 1 : git init + repo GitHub
- 2 : ENG-1 aggregateGraphs + ENG-2 verifyDagAcyclic
- 3 : ENG-3 provisionWorkspace + ENG-4 CLI
- 4 : B1 StdoutFormatter [CTX]/[PLAN]/[RESULT] dans codebase-gradle/rag
- 5 : ENG-5 openapi.yaml + apiSchema + ENG-6 chatbot.sh
- 6 : ENG-7 Integration engine.sh (api/chatbot/health) + ENG-8 health.sh

== Prochaine etape — Session 007

Option A — Pivot codebase-gradle : US-9.10 CompositeContextBuilder
Option B — ENG-9 : Vrai serveur HTTP pour /health (Ktor ou http4k minimal)
Option C — ENG-10 : Provisionnement SaaS via API (POST /provision)

== Actif des sessions 5-6

* `openapi.yaml` — contrat (3 paths, 6 schemas)
* `apiSchema` — validation Kotlin pure
* `chatbot.sh` — REPL Ollama, prompt N3 francais
* `engine.sh` — 8 commandes (info/verify/graph/provision/api/contract/chatbot/health)
* `health.sh` — JSON HealthResponse (21 plugins, 21k nodes, DAG OK)

== Etat Actuel

[source]
----
CLI: ./engine.sh <command>
  info       → Engine status + DAG registry
  verify     → verifyDagAcyclic
  graph      → aggregateGraphs
  provision  → provisionWorkspace
  api|contract → apiSchema (openapi.yaml validation)
  chatbot [model] → REPL Ollama
  health     → JSON health endpoint mock
  tasks      → ./gradlew tasks --group engine
----

== Decisions Actees

* Engine build script only — pas de src/main/kotlin
* Secrets depuis configuration/
* Zero Groovy — apiSchema en Kotlin pur
* StdoutFormatter dans codebase-gradle/rag (N1)

[.text-right]
*Session 2026-05-11 — generee par la procedure de fin de session 006.*
